<h3>以下の内容で送信します。</h3>

<%= form_for(@photo) do |f| %>
  <%= f.hidden_field :title %>
  <%= f.hidden_field :content %>
  <p>タイトル：<%= @photo.title %></p>
  <p>本文：<%= @photo.content %></p>
  <%= f.submit "投稿する" %>
<% end %>
